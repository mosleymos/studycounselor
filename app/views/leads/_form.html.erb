<%= form_for(@lead) do |f| %>
  <% if @lead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lead.errors.count, "error") %> prohibited this lead from being saved:</h2>

      <ul>
      <% @lead.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br>
    <%= f.text_field :card_id %>
  </div>
  <div class="field">
    <%= f.label :program_id %><br>
    <%= f.text_field :program_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :recall %><br>
    <%= f.check_box :recall %>
  </div>
  <div class="field">
    <%= f.label :document %><br>
    <%= f.check_box :document %>
  </div>
  <div class="field">
    <%= f.label :approved_by_school %><br>
    <%= f.check_box :approved_by_school %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
